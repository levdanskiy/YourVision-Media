# ALMANAC: TABLE - July Logo Notes

Asset:

- `ALMANAC_TABLE_JULY_logo.svg`
- `ALMANAC_TABLE_JULY_logo_1024.png` if generated successfully

Design rule:

- exact public name visible: `ALMANAC: TABLE`;
- archive-era name `ALMANAC: MARGINALIA` is omitted;
- table/plate geometry instead of myth/calendar symbols;
- carmine and olive accents so July does not read as a one-hue dark palette;
- editorial restraint, not cozy kitchen illustration.

Use:

- Telegram avatar or promo tile for the July recipe-only pivot;
- crop center-safe: the wordmark and table mark sit inside the central circle;
- if Telegram circular crop removes bottom text, the core `ALMANAC / TABLE` remains readable.
